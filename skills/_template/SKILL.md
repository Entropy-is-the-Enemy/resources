---
name: skill-name
description: One or two sentences naming the concrete triggers. "Use when the user wants to X, or mentions Y, Z. Do NOT use for W." This description is what Claude reads to decide whether to load the skill, so make the triggers specific.
---

# Skill Name

One line on what this skill does and the outcome it produces.

## When this applies

- Trigger phrases and situations that should fire this skill.
- What's explicitly out of scope (and, if relevant, which other skill owns it).

## How it works

1. First step — what to gather or read.
2. Second step — the core move.
3. Verification — how to check the output is right before finishing.

## Rules

- Load-bearing constraints that must always hold.
- Things to never do.

## Example

A short worked example: input, what the skill does with it, and the shape of the output.
